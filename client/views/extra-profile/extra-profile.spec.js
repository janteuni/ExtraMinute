'use strict';

describe('Controller: ExtraProfileCtrl', function () {

  beforeEach(module('extraMinute'));

  var ExtraProfileCtrl;

  beforeEach(inject(function ($controller) {
    ExtraProfileCtrl = $controller('ExtraProfileCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
