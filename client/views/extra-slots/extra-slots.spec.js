'use strict';

describe('Controller: ExtraSlotsCtrl', function () {

  beforeEach(module('extraMinute'));

  var ExtraSlotsCtrl;

  beforeEach(inject(function ($controller) {
    ExtraSlotsCtrl = $controller('ExtraSlotsCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
