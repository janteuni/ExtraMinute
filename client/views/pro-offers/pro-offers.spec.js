'use strict';

describe('Controller: ProOffersCtrl', function () {

  beforeEach(module('extraMinute'));

  var ProOffersCtrl;

  beforeEach(inject(function ($controller) {
    ProOffersCtrl = $controller('ProOffersCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
