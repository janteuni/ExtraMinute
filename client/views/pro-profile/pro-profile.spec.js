'use strict';

describe('Controller: ProProfileCtrl', function () {

  beforeEach(module('extraMinute'));

  var ProProfileCtrl;

  beforeEach(inject(function ($controller) {
    ProProfileCtrl = $controller('ProProfileCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
